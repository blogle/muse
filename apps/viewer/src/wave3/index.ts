/** Prewired Wave 3 component ownership slots; implementations remain follow-up work. */
export { wave3RenderChannels } from './renderChannels';
export { wave3RawInspector } from './rawInspector';
export { wave3CausalOverlays } from './causalOverlays';

export const wave3Ownership = 'contracts-only' as const;
