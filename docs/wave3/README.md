# Wave 3 ownership

`CAUSAL-WORLD-MODEL.md` is normative. Contract/type/compiler seams are coordinator-owned and frozen here. Follow-up operator workers own their respective implementation/test modules; recipe workers own declarative YAML under their assigned target-model directories; validation workers own paired-comparison implementations and fixtures; viewer workers own generic viewer integration. Workers must not modify central descriptors or compiler schemas without coordinator approval.
