[package]
name = "muse"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A minimal Rust crate bootstrap"
license = "MIT OR Apache-2.0"

[profile.dev]
opt-level = 0
incremental = true
codegen-units = 256
debug = "line-tables-only"

[profile.dev.package."*"]
debug = false
