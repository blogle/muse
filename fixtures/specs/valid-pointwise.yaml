version: 1
inputs:
  source: scalar_field
parameters:
  multiplier: {type: scalar, default: 2.0}
programs:
  generate:
    nodes:
      transform:
        op: pointwise
        inputs:
          x: $input.source
          scale: $param.multiplier
        expr: x * scale
